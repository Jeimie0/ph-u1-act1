import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.u1.ac1',
  appName: 'ph-u1-ac1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
